import XCTest
@testable import Reward_SDK_iOS_SwiftPackage

final class Reward_SDK_iOS_SwiftPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Reward_SDK_iOS_SwiftPackage().text, "Hello, World!")
    }
}
